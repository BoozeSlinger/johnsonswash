# Quote form delivery via Resend (https://resend.com).
# Without these, the form shows a "please call us" fallback instead of sending.
RESEND_API_KEY=
QUOTE_TO_EMAIL=
# Must be an address on a domain verified in Resend.
QUOTE_FROM_EMAIL="Performance One <quotes@your-domain.com>"
